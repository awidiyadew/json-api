{
  "name": "contact-json",
  "version": "1.0.0",
  "description": "Contact JSON API",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server --watch $DB"
  },
  "author": "awidiyadew",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.14.2"
  }
}
